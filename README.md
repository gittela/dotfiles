# dotfiles

## Install

    curl -s https://raw.githubusercontent.com/gittela/dotfiles/master/setup.sh | zsh
    chsh -s `which zsh`
    vim +PluginInstall +qall

## Update

    dotupdate
    vim +PluginInstall +qall
